spring:
  datasource:
    url: jdbc:mysql://localhost:3307
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: daniel
  jpa:
    hibernate:
      ddl-auto: validate
  liquibase:
    contexts: prod
